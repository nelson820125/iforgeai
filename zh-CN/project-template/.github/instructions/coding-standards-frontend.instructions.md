---
description: "Use when writing, reviewing or modifying frontend code. Project-specific overrides for frontend standards."
applyTo: ["**/*.vue", "**/*.ts", "**/*.tsx", "**/*.scss", "**/*.css"]
---

# 前端编码规范（项目覆盖）

> 本文件覆盖全局规范中需要针对本项目调整的部分。
> 全局规范见用户级 `coding-standards-frontend.instructions.md`。

## 技术栈版本

> 注明本项目确认使用的框架和库版本，工程师不得引入此处未列出的新依赖。
>
> 示例：
> ```
> Vue: 3.4.x
> TypeScript: 5.x
> Pinia: 2.x
> Element Plus: 2.x
> ```

（待填写）

## UI 组件库约定

> 说明本项目使用的组件库及其使用约定（如：优先使用库组件，禁止自行实现已有组件等）。

（待填写）

## 路由与目录结构

> 说明本项目的前端目录结构约定（如：views/ 与 components/ 的职责划分）。

（待填写）

## 项目特有规则

> 追加本项目特有的前端约定，格式：
> `- {规则描述}`

（待填写）
